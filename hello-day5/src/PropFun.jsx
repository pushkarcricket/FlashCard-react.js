import React from 'react'

export default function PropFun(...props) {
  return (
    <div>
        <h1>{item.props}</h1>
        <p>{descriptio.props}</p>
        <h3>{price.props}</h3>
        <button>{btn.props}</button>
    </div>
  )
}
